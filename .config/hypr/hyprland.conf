monitor=,highres,auto,2
input {
    kb_layout=us
    kb_variant=
    kb_model=
    kb_options=
    kb_rules=
    follow_mouse=1
    touchpad {
        disable_while_typing=0
    }
}

gestures {
    workspace_swipe=1
    workspace_swipe_invert=1
}



animations {
    enabled=1
    # bezier=overshot,0.05,0.9,0.1,1.1
    bezier=overshot,0.13,0.99,0.29,1.1
    animation=windows,1,4,overshot,slide
    animation=border,1,10,default
    animation=fade,1,10,default
    animation=workspaces,1,3,overshot,slide
}

general {
    sensitivity=1.0 # for mouse cursor

    gaps_in=2
    gaps_out=1
    border_size=3
    col.active_border= rgb(90234E) 
    col.inactive_border= 0x55555555

    apply_sens_to_raw=0 # whether to apply the sensitivity to raw input (e.g. used by games where you aim using your mouse)
}

decoration {
    rounding=5
    blur=0
    blur_size=3 # minimum 1
    blur_passes=1 # minimum 1, more passes = more resource intensive.
    # Your blur "amount" is blur_size * blur_passes, but high blur_size (over around 5-ish) will produce artifacts.
    # if you want heavy blur, you need to up the blur_passes.
    # the more passes, the more you can up the blur_size without noticing artifacts.
}


dwindle {
    pseudotile=0 # enable pseudotiling on dwindle
}

# example window rules
# for windows named/classed as abc and xyz
#windowrule=move 69 420,abc
#windowrule=size 420 69,abc
#win=tile,xyz
#windowrule=float,abc
#windowrule=pseudo,abc
#windowrule=monitor 0,xyz
exec-once=dbus-update-activation-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once=swaybg -m fill -i Pictures/wallp.png
exec-once=foot --server
exec-once=waybar
exec-once= env MESA_GL_VERSION_OVERRIDE=3.3 MESA_GLSL_VERSION_OVERRIDE=330 MESA_GLES_VERSION_OVERRIDE=3.1 MOZ_ENABLE_WAYLAND=1 SDL_VIDEODRIVER=wayland MOZ_ENABLE_WAYLAND=1 firefox-wayland
exec-once=/usr/libexec/kf5/polkit-kde-authentication-agent-1
# example binds
bind=SUPER,RETURN,exec,footclient
bind=SUPERSHIFT,E,exit,
bind=SUPER,SPACE,togglefloating,
bind=SUPER,F,fullscreen,
bind=SUPERSHIFT,Q,killactive,
bind=SUPER,D,exec,wofi --width=400 --show drun
bind=SUPER,left,movefocus,l
bind=SUPER,right,movefocus,r
bind=SUPER,up,movefocus,u
bind=SUPER,down,movefocus,d
bind=CONTROLSHIFT,4,exec,grim -g "$(slurp)" - | wl-copy
exec-once=dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
bind=SUPER,O,workspace,e-1
bind=SUPER,P,workspace,e+1

bind=SUPERSHIFT,L,exec,swaylock
bind=,XF86LaunchA,exec,light -s sysfs/leds/kbd_backlight -U 10%
bind=,XF86Search,exec,light -s sysfs/leds/kbd_backlight -A 10%
bind=,XF86AudioRaiseVolume,exec,pactl set-sink-volume @DEFAULT_SINK@ +10% 
bind=,XF86AudioLowerVolume,exec,pactl set-sink-volume @DEFAULT_SINK@ -10%
bind=,XF86MonBrightnessUp,exec,light -A 5%
bind=,XF86MonBrightnessDown,exec,light -U 5%
bind=,XF86Suspend,exec,light -S 100

bind=ALT_L,left,moveactive,l
bind=ALT_L,right,moveactive,r
bind=ALT_L,up,moveactive,u
bind=ALT_L,down,moveactive,d

bind=SUPERSHIFT,left,movewindow,l
bind=SUPERSHIFT,right,movewindow,r
bind=SUPERSHIFT,up,movewindow,u
bind=SUPERSHIFT,down,movewindow,d

bind=SUPER,1,workspace,1
bind=SUPER,2,workspace,2
bind=SUPER,3,workspace,3
bind=SUPER,4,workspace,4
bind=SUPER,5,workspace,5
bind=SUPER,6,workspace,6
bind=SUPER,7,workspace,7
bind=SUPER,8,workspace,8
bind=SUPER,9,workspace,9

bind=SUPERSHIFT,1,movetoworkspacesilent,1
bind=SUPERSHIFT,2,movetoworkspacesilent,2
bind=SUPERSHIFT,3,movetoworkspacesilent,3
bind=SUPERSHIFT,4,movetoworkspacesilent,4
bind=SUPERSHIFT,5,movetoworkspacesilent,5
bind=SUPERSHIFT,6,movetoworkspacesilent,6
bind=SUPERSHIFT,7,movetoworkspacesilent,7
bind=SUPERSHIFT,8,movetoworkspacesilent,8
bind=SUPERSHIFT,9,movetoworkspacesilent,9

bindm=SUPER,mouse:272,movewindow
bindm=ALT,mouse:272,resizewindow
